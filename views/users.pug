extends layout

block scripts
  script(src='/javascripts/groups.js')
  script.
    var groups = getGroups();
    console.log(1);
    console.log(groups);

block content
  link(rel='stylesheet', href='/stylesheets/users-override.css')
  h1 profile
  h2 my groups
  if groups > 0
    table#groups
      thead
        tr
          th
      tbody
        for res in groups
          tr
            td= groupName
  else
    h2 it's a little quiet in here
    h2 get some friends to join meet.up!